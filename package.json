{
  "name": "@superbenefit/porch",
  "version": "0.1.0",
  "description": "MCPorch - SuperBenefit MCP Server Framework Reference Implementation",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "types": "wrangler types src/env.d.ts --include-runtime false",
    "test": "vitest run"
  },
  "dependencies": {
    "agents": "^0.3.6",
    "hono": "^4.11.8",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.0.0",
    "typescript": "^5.0.0",
    "vitest": "^3.0.0",
    "wrangler": "^4.0.0"
  },
  "overrides": {
    "@modelcontextprotocol/sdk": "^1.26.0"
  }
}
